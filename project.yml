name: EscritaCriativa
options:
  bundleIdPrefix: com.rniepce
  deploymentTarget:
    iOS: "17.0"
  createIntermediateGroups: true
  xcodeVersion: "15.4"
  usesTabs: false
  indentWidth: 4

targets:
  EscritaCriativa:
    type: application
    platform: iOS
    deploymentTarget: "17.0"
    sources:
      - path: EscritaCriativa
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: com.rniepce.escritacriativa
        SWIFT_VERSION: "5.9"
        INFOPLIST_FILE: EscritaCriativa/Info.plist
        TARGETED_DEVICE_FAMILY: "1"
        MARKETING_VERSION: "1.0"
        CURRENT_PROJECT_VERSION: "1"
        CODE_SIGN_STYLE: Automatic
        DEVELOPMENT_TEAM: ""
